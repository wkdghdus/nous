# Schemas Signpost

Use this directory for versioned schemas that define portable data shapes for Obsidian frontmatter, graph nodes, graph edges, claims, and generated exports.

Direct children:

- `graph.schema.json` - initial JSON schema draft for graph exports.
- `note-frontmatter.schema.yaml` - initial YAML-shape notes for Obsidian frontmatter.
