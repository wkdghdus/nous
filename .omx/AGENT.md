# OMX Signpost

This directory holds local OMX workflow artifacts. Runtime state and logs are ignored; durable plans may be committed when they explain repository setup or implementation intent.

Direct children:

- `plans/` - durable planning artifacts.
- `state/` - ignored runtime state.
- `logs/` - ignored runtime logs.
