# Ralph Context Snapshot: Nous M7B

Timestamp: 2026-08-09T01:57:10Z

## Task statement

Implement only `nous-m7` stage M7B using the six-stage draft package under `.omx/plans/nous-m7-six-stage-draft/`.

## Desired outcome

Create a side-effect-free, presentation-free Ruby Nous Core for existing read-heavy behavior, and route graph export, report generation, and review list/show/report inspection through it without changing current CLI contracts or fixed-time output bytes.

## Known facts and evidence

- M7A work is present as uncommitted changes in the working tree and must be preserved.
- The Ralph planning gate is satisfied by the umbrella M7 PRD and the M7B test specification in the supplied plan package.
- The current product is dependency-free Ruby implemented under `scripts/`; no `lib/` directory exists yet.
- The M7B entry gate requires the M7A characterization plus all M2-M6 tests and lint to pass before implementation.
- M7B owns read-only extraction only; current mutation paths remain in the CLI scripts.
- The required verification includes direct read-core tests, all existing script tests, `make test`, `make lint`, `git diff --check`, static scope checks, and architect approval.

## Constraints

- Follow the root `AGENT.md`, M7 umbrella PRD, shared contract, execution map, M7B plan, and M7B test specification.
- Preserve the existing uncommitted M7A changes; do not revert or overwrite them.
- Implement M7B only. Do not add M7C locks/writers/mutation extraction, M7D agent reads, M7E writes/idempotency, or M7F MCP dependencies/server code.
- Preserve exact command behavior, error prefixes/statuses, environment precedence, output paths, and fixed-time graph/report bytes.
- Core code must accept explicit inputs, remain silent and side-effect free, avoid `ARGV`/CLI environment access, and expose stable namespaced errors.
- Use temporary synthetic fixtures only; do not copy private vault data into tests.
- Add no dependencies.

## Unknowns and open questions

- Whether the current uncommitted M7A baseline passes all required checks as-is.
- Which parser/normalization behaviors are genuinely compatible across graph, report, and review code and which need explicit modes.
- The smallest cohesive core file structure that preserves behavior without speculative abstractions.
- Whether review read extraction can be isolated cleanly from mutation helpers without broad changes to `review_queue.rb`.

## Likely codebase touchpoints

- `lib/AGENT.md`
- `lib/nous.rb`
- `lib/nous/AGENT.md`
- cohesive files under `lib/nous/` for errors/time, parsing/records/normalization, graph, report, and review reads
- `scripts/export_graph.rb`
- `scripts/generate_nous_report.rb`
- read-only paths in `scripts/review_queue.rb`
- `scripts/test_nous_read_core.rb`
- `Makefile`
- `scripts/AGENT.md`
- `.omx/context/AGENT.md`
