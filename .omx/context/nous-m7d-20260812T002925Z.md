# Ralplan Context Snapshot: nous-m7d

- Task statement: Plan Nous milestone M7D, Agent-Safe Read Operations, through the non-interactive RALPLAN consensus workflow.
- Desired outcome: Produce an approved, implementation-ready M7D plan plus PRD and test specification for mutation-free `status`, `list_records`, `read_record`, and `read_source_text` Nous Core operations; do not implement M7D.
- Known facts/evidence:
  - The existing staged draft is `.omx/plans/nous-m7-six-stage-draft/m7d-agent-safe-read-operations-plan.md`, with verification contract `test-spec-m7d-agent-safe-read-operations.md`.
  - M7D depends on M7C and unlocks M7E. Current HEAD is `1fc7f85`, the worktree is clean, and M7C core modules/tests are present.
  - `lib/nous.rb` provides the shared error/record/parser helpers and preserves reviewed/canonical-only graph/report discovery.
  - `lib/nous/relationship_integrity.rb` already contains known record directories, duplicate grouping, and lifecycle-related logic that should be generalized rather than duplicated.
  - `lib/nous/path_guard.rb` and `lib/nous/vault_lock.rb` provide the M7C confinement and locking primitives.
  - M2 stores source text in an `Observed Content` note section; M6 stores copied payload metadata including vault-relative path, SHA-256, and bytes.
- Constraints:
  - M7D only: no candidate writes, request IDs, idempotency, schema/template changes, MCP, semantic retrieval, persistent index, arbitrary paths, binary reads, review mutations, frontend, network, or model calls.
  - Preserve graph/report reviewed-only behavior and every existing M2-M6 CLI/vault contract.
  - All returned record/user content is bounded and labeled `untrusted_data`; absolute external paths and unknown frontmatter fields are not exposed.
  - Public operations are mutation-free except the already documented ignored lock-file behavior, use stable IDs, reject symlink/path escapes, and return stable sanitized errors.
  - No new dependencies; use synthetic temporary fixtures; do not mutate repository vault content.
- Unknowns/decisions the consensus plan must lock:
  - Malformed records in `status`: bounded warnings versus whole-operation failure.
  - Duplicate IDs in listing: selected-scope failure semantics.
  - Whether direct reads of retired unique records remain allowed.
  - Structured unavailability versus errors for unsupported source kinds.
  - EOF offset behavior and legacy M6 payloads missing checksum/byte metadata.
  - Exact lexical fields and whether body matching scans a bounded derived excerpt.
- Likely codebase touchpoints:
  - `lib/nous.rb`; `lib/nous/relationship_integrity.rb`; new cohesive read/index modules under `lib/nous/`.
  - `scripts/test_nous_agent_reads.rb`; `Makefile`; `docs/agent/AGENT.md`; `docs/agent/read-contract.md`; signposts as needed.
  - Planning outputs under `.omx/plans/`, grounded in the existing M7 staged package.
- Recommended starting architecture: one authoritative in-memory record index/lifecycle layer generalized from relationship integrity; unchanged reviewed-only graph/report loaders; curated envelope plus status/query/record/source services; one public shared-lock boundary with internal helpers receiving the already-built index.
- Verification expectation: focused M7D suite, all M2-M7C regression tests, `make test`, `make lint`, `git diff --check`, mutation/privacy/symlink/bounds/determinism scans, and independent verifier review during execution.
