# Ralph Context Snapshot: nous-m7c

- Task statement: Implement Nous milestone M7C only, using the planning package at `.omx/plans/nous-m7-six-stage-draft`.
- Desired outcome: Every existing M2-M6 mutation is routed through Nous Core with vault path confinement, one vault-scoped OS lock, atomic writes, coordinated M6 rollback, collision safety, and relationship approval endpoint integrity while preserving all current CLI and vault contracts.
- Known facts/evidence:
  - The M7C plan and test specification exist at `.omx/plans/nous-m7-six-stage-draft/m7c-mutation-core-and-vault-safety-plan.md` and `test-spec-m7c-mutation-core-and-vault-safety.md`.
  - M7B produced a side-effect-free `lib/nous.rb` read core; mutations still live in `scripts/ingest_text.rb`, `scripts/ingest_artifact.rb`, and `scripts/review_queue.rb`.
  - Current Makefile runs CLI characterization, read-core, ingestion, review, graph, and report tests.
  - The worktree was clean at intake.
- Constraints:
  - M7C only: no agent read/write APIs, request IDs, idempotency, MCP, Gemfile changes, frontend/model/search/database work, or schema/template expansion.
  - Preserve CLI invocation, stdout/stderr prefixes, filenames, frontmatter, Markdown bodies, time/environment precedence, collision suffixes, graph/report bytes, and existing vault compatibility.
  - Keep `$EDITOR` execution in the CLI adapter.
  - Use synthetic temporary fixtures only; do not mutate repository vault records.
  - No new dependencies; keep diffs surgical and reversible.
- Unknowns/open questions:
  - Whether all Gate B baseline tests are green before implementation (baseline run pending).
  - Exact extraction boundaries inside the existing monolithic `lib/nous.rb` and mutation scripts.
  - Whether shared locks can be applied to graph/report generation without byte/output drift; process tests will decide.
- Likely codebase touchpoints:
  - `lib/nous.rb`, new cohesive files under `lib/nous/` for path/lock/write/transaction/collision/ingestion/review integrity.
  - `scripts/ingest_text.rb`, `scripts/ingest_artifact.rb`, `scripts/review_queue.rb`, and possibly graph/report adapters for coherent locking.
  - `scripts/test_nous_mutation_core.rb`, existing M2-M6 regression tests, `Makefile`, `.gitignore`, and relevant signposts/docs.
- Verification contract: Run `ruby scripts/test_nous_mutation_core.rb`, all M7B/M2-M6 tests, `make test`, `make lint`, `git diff --check`, scope/privacy/temp-file scans, independent architect verification, changed-files-only deslop, then full regression re-verification.
